import { Meta, Markdown } from '@storybook/addon-docs/blocks';
import content from '../../docs/guides/generating-ui.md?raw';

{/* Rendered from docs/guides/generating-ui.md so the repo and the docs site
    cannot disagree — ADR 0002. */}

<Meta title="Guides/Generating UI" />

<Markdown>{content}</Markdown>
