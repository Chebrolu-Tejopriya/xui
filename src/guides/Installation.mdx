import { Meta, Markdown } from '@storybook/addon-docs/blocks';
import content from '../../docs/guides/installation.md?raw';

{/* Rendered from docs/guides/installation.md so the repo and the docs site cannot
    disagree — ADR 0002, derive artefacts rather than hand-maintaining them. */}

<Meta title="Guides/Installation" />

<Markdown>{content}</Markdown>
