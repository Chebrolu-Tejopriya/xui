{
  "_comment": "Authored knowledge — the rules an agent needs that are NOT derivable from component signatures. Merged into xui.manifest.json by scripts/gen-manifest.mjs. This is the code-side twin of xemantics' rules.js.",

  "principles": [
    "Never use a raw hex, rgb(), or a primitive token (--gray-04) in product code. Always use a semantic token (--surface-tertiary). Primitives exist only to define semantics.",
    "Semantic tokens are theme-reactive: the same token resolves light and dark automatically. Hardcoding a color breaks dark mode.",
    "Compose from existing components before writing new markup. If a pattern needs a component that does not exist, flag it rather than hand-rolling a one-off.",
    "Figma strokes take no layout space; CSS borders do. When a spec gives an explicit height AND a border, compensate the padding or draw the divider with an inset box-shadow.",
    "Text sitting on a solid brand/status surface must use content-absolute-white, never a theme-reactive content token (it inverts in dark and disappears)."
  ],

  "tokenUsage": {
    "page background": "surface-primary",
    "card / panel / table row background": "surface-raised",
    "table header, hovered row, subtle button": "surface-secondary",
    "inset wells, disabled fills, inactive meter bars": "surface-tertiary",
    "selected row / selected nav item": "surface-brand-secondary",
    "primary action fill": "surface-brand-primary (hover surface-brand-solid, loading/disabled surface-brand-subtle)",
    "destructive action fill": "surface-error-primary (hover surface-error-solid)",
    "body & heading text": "content-primary",
    "secondary / supporting text": "content-secondary",
    "placeholder, meta, icon-beside-text": "content-tertiary",
    "faintest icons & helper glyphs": "content-quaternary",
    "text on a solid brand/status fill": "content-absolute-white",
    "default control border": "border-primary",
    "table container border + row dividers, muted/loading borders": "border-secondary",
    "faintest separators": "border-tertiary",
    "focus ring / active control border": "border-brand",
    "status & category pills": "the label-* pairs (label-<tone>-bg with label-<tone>-content)"
  },

  "componentRules": {
    "Table": [
      "Header row uses surface-secondary with a border-secondary underline; header cells are Subtitle/2 (500 weight), padding 16px 12px.",
      "Body rows are surface-raised, 52px tall; cells are Body/2, padding 16px 8px.",
      "Row hover is surface-secondary (grey); row selected is surface-brand-secondary (blue tint). They are DIFFERENT states — selected wins when both apply.",
      "Container has a border-secondary 1px border, radius-mid (8px), and overflow:hidden to clip rows to the corners.",
      "Dividers are inset box-shadows, not borders, so rows stay exactly 52px.",
      "Give a cell an explicit `width` for fixed columns; omit it for equal-share flexible columns."
    ],
    "Button": [
      "Heights are 44 / 36 / 32 for large / medium / small; radius-sm (6px).",
      "Icon-only is its own treatment (surface-raised + border-primary square 36/32/30 or circle 44/40/38), NOT the active variant squared off — pass the icon as children and always set aria-label.",
      "Loading lightens the fill to the family's `subtle` step and disables interaction.",
      "Secondary is the warning/orange family, not a grey — this is intentional in XUI."
    ],
    "Badge": [
      "Use the label-* variants for status and category pills; the accent-* variants are for softer inline tags.",
      "Badges are 20px tall; the pill's border is included in that height."
    ],
    "Icons": [
      "Four tones: outlined, solid, dualtone, dualtone-selected. Outlined/solid inherit currentColor; dualtone defaults to content-tertiary and dualtone-selected to content-brand-primary.",
      "Set --xui-icon-knockout to the surface an icon sits on if its cut-outs need to blend with something other than surface-raised."
    ]
  },

  "layoutPatterns": {
    "data table page": "PageHeader (title + primary action) → filter/search toolbar → Table → Pagination. Selection surfaces a floating action bar.",
    "form": "Label above field, helper or error text below, 20px between fields; primary action bottom-right, secondary to its left.",
    "empty state": "Center a short message in a single full-width table cell using content-tertiary."
  },

  "antiPatterns": [
    "Hardcoding #ffffff for a card background instead of surface-raised — it stays white in dark mode.",
    "Using content-primary for a label on a brand-filled button — invisible in one of the two themes.",
    "Adding a CSS border to a fixed-height element without shaving the padding — renders 2px taller than the spec.",
    "Building a bespoke table with divs when Table/TableRow/TableCell already encode the spec.",
    "Using surface-secondary for a selected row — that is the hover treatment; selected is surface-brand-secondary."
  ]
}
